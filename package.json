{
  "name": "github-api-client",
  "version": "1.0.0",
  "description": "Mini GitHub API client",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": ["github", "api"],
  "author": "mharyst",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^4.5.0",
    "if-env": "^1.0.0",
    "preact-cli": "^1.4.1"
  },
  "dependencies": {
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0",
    "preact-router": "^2.5.6"
  }
}
